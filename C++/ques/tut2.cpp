#include<iostream>
using namespace std;
int main()
{   
    int a=4,b=7;
    cout<<"operators inn c++"<<endl;
    cout<<"Following are the operators in C++"<<endl;
    //arithmatic operators
    // cout<<"the value of a+b is "<<a+b<<endl;
    // cout<<"the value of a-b is "<<a-b<<endl;
    // cout<<"the value of a*b is "<<a*b<<endl;
    // cout<<"the value of a/b is "<<a/b<<endl;
    // cout<<"the value of a%b is "<<a%b<<endl;
    // cout<<"the value of a--is "<<a--<<endl;
    // cout<<"the value of a++ is "<<a++<<endl;
    // cout<<"the value of ++a "<<++a<<endl;
    // cout<<"the value of --a "<<--a<<endl;   

    // // // comparison aoperators
    //  cout<<"the value of a<b "<<(a<b)<<endl;     
    // cout<<"the value of a>b "<<(a>b)<<endl;   
    // cout<<"the value of a==b "<<(a==b)<<endl;   
    // cout<<"the value of a<=b "<<(a<=b)<<endl;   
    // cout<<"the value of a>=b"<<(a>=b)<<endl;     
    // cout<<"the value of a!=b "<<(a!=b)<<endl;   


    // logiacal operators
    cout<<"Following are logical operators";
    cout<<"the value of ((a==b) && (a>b) is "<<((a==b) && (a<b))<<endl;   // logical and operator
    cout<<"the value of ((a==b) || (a>b) is "<<((a==b) || (a<b))<<endl;     // logical or operator

  








    
    return 0;


}